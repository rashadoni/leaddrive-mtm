# Как изменения доезжают до полевых агентов

Тот же порядок, что и в `leaddrive-v2` (там же полный текст с историей:
`docs/DELIVERY-ARCHITECTURE.md`). Действует одинаково для всех сессий: Claude,
Codex, любых будущих.

Владелец работает один и не читает диффы. От него требуется одно: перед
релизом ответить «давай» на короткий список.

## Слой 1. В `main` только через pull request

Прямой пуш запрещён правилом репозитория. Обязательные проверки — `scope` и
`tests` из `.github/workflows/ci.yml`: jest и `tsc --noEmit` приложения.
Workflow запускается на каждом PR без фильтра по путям; `scope` решает, менялось
ли приложение, и при PR только с текстом `tests` пропускается — пропуск
засчитывается как пройденная проверка. Обязательны обе: упавший `scope` иначе
«пропустил» бы `tests` в зелёный. `build-apk` остаётся с фильтром по путям и
обязательным не делается — это сборка и публикация, а не проверка.

Настраивается не руками, а скриптом `scripts/ci/configure-main-protection.sh`.

Одобрения ревьюера не требуются: владелец работает один, и правило, которое
некому выполнить, останавливает доставку вместо того, чтобы её защищать.
`enforce_admins` выключен осознанно — это аварийный выход, и каждый его случай
стоит называть вслух.

## Слой 2. Ревью

Платного ИИ-ревьюера в CI нет (решение владельца 2026-09-14, то же, что для
`leaddrive-v2` 2026-09-11). Прежний `agent-review` без ключа `ANTHROPIC_API_KEY`
был зелёным на каждом PR и ничего не разбирал — при этом оставался единственной
обязательной проверкой, так что тесты не требовались вовсе. Его заменили тесты
(слой 1). Проверка вида на устройстве — `MTMobileApp/scripts/e2e-adb.sh` и
`MTMobileApp/docs/e2e-adb-checklist.md`: то, чего не видят тесты.

## Слой 3. Сборка

APK собирается в CI (`build-apk.yml`), подпись fail-closed: релиз никогда не
выходит с debug-ключом, иначе установленное у агента приложение не обновится.

## Слой 4. Слово владельца

Единственное, чего машина не может: «я такого не заказывал». Перед мержем того,
что увидит агент в поле, показать короткий список — по одной строке на изменение,
человеческим языком, без имён файлов — и дождаться «давай».

Список нужен только для видимого. Правки тестов, документации и внутренних
скриптов в него не попадают.

## Что обязан делать любой агент

1. Не пушить в `main`. Только PR.
2. Не мержить красное и не ослаблять проверку, чтобы пройти.
3. Не мержить, пока `scope` и `tests` не зелёные.
4. Перед мержем видимого — список из слоя 4 и «давай».
5. Своё же ревью не считается.
